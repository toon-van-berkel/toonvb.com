# New styles foundation

- base = reset, typography and global foundations
- tokens = colors, spacing, breakpoints and design tokens
- layout = reusable layout primitives
- utilities = small generic utility styles

This is only a scaffold. The current application still uses the existing SCSS locations.
