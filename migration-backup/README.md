# Migration backup

Temporary snapshot of important content, translations, types and architecture notes. This folder is not runtime code and must never be imported. Existing files were copied, not moved or deleted. It can be removed after migration is complete and verified.
